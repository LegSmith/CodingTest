# CLAUDE.md

프로그래머스 코딩테스트 문제 풀이를 기록하는 저장소입니다. 아래는 이 저장소에서 "오늘 푼 문제 README에 업데이트해줘" 같은 요청을 받았을 때 따라야 할 규칙입니다.

## 저장소 구조

```
src/<stage>/dayNN/SolutionMM.java   # 풀이 코드
docs/<stage>.md                      # 단계별 상세 풀이 목록 (일차별 <details> 표)
README.md                            # 전체 진행 현황 요약 (3단계 진행률 표)
```

- `stage` 값: `basic`(1단계, 코딩 기초 트레이닝) / `beginner`(2단계, 코딩테스트 입문, 아직 시작 전) / `algorithm-kit`(3단계, 알고리즘 고득점 Kit, 아직 시작 전)
- 각 단계의 전체 문제 수(2026-08 기준): 기초 트레이닝 124 / 입문 100 / 고득점 Kit 47
- 클래스는 `package <stage>.dayNN;` 소속이며, 클래스 상단 Javadoc의 `<h2>제목</h2>` 태그에 문제 제목이 들어 있습니다. 이 제목을 그대로 `docs/<stage>.md`의 표에 사용하세요.

## "오늘 푼 문제 업데이트해줘" 요청을 받으면

1. `git status` / `git diff`로 새로 추가되거나 수정된 `src/**/Solution*.java` 파일을 찾습니다. 애매하면 사용자에게 어떤 문제를 반영할지 확인합니다.
2. 각 파일의 경로에서 단계(`stage`)와 일차(`dayNN`), 문제 번호(`SolutionMM`)를 파악하고, Javadoc의 `<h2>` 태그에서 문제 제목을 가져옵니다.
3. `docs/<stage>.md`를 수정합니다.
   - 이미 해당 일차의 `<details>` 블록이 있으면 그 안의 표에 행을 추가합니다.
   - 새로운 일차라면 파일 맨 위에 새 `<details open>` 블록을 추가하고, 기존에 `open`이던 블록에서는 `open` 속성을 제거합니다 (항상 최신 일차 하나만 펼쳐진 상태 유지).
   - 표는 기본적으로 `# | 문제` 2열입니다. 특이사항(사용한 메서드, 부분 실패한 테스트케이스 등)이 있는 문제가 하나라도 있으면 `비고` 열을 추가하고 나머지 행은 빈 칸으로 둡니다.
   - 링크는 `docs/` 기준 상대 경로이므로 `../src/<stage>/dayNN/SolutionMM.java` 형식을 씁니다.
4. `README.md`의 진행 현황 표를 갱신합니다.
   - 해당 단계 행의 `진행` 칸을 `풀이수 / 전체수` + 진행률 바 + 퍼센트로 갱신합니다.
   - 진행률 바는 대괄호 안에 `█`(채움) + `░`(빈칸) 총 20칸입니다. 채움 칸 수 = `round(퍼센트 / 100 * 20)`.
   - `상태` 칸: 0문제 = `⬜ 시작 전`, 1문제 이상 ~ 전체 미만 = `🟢 진행 중`, 전체 다 풀면 = `✅ 완료`.
5. 다른 단계의 문서나 진행률은 건드리지 않습니다.
6. 별도 요청이 없는 한 git commit/push는 하지 않습니다 (파일만 수정).

## 새 단계 시작 시

2단계(입문) 또는 3단계(고득점 Kit)를 시작하게 되면:
- `src/beginner/day01/...` 또는 `src/algorithm-kit/<카테고리>/...` 형태로 코드를 추가하고,
- `docs/beginner.md` 또는 `docs/algorithm-kit.md`를 `docs/basic-training.md`와 같은 형식(`<details>` 표)으로 새로 만든 뒤,
- `README.md`의 "풀이 기록" 목록에서 해당 단계의 `*(시작 전)*` 링크를 실제 문서 링크로 바꿉니다.

## 커밋 메시지

형식: `[Type] : <emoji> <한글 요약>` (제목 한 줄). 필요 시 빈 줄 뒤 본문에 변경 이유·핵심을 적습니다.

- Type(대문자 고정): `Feat` `Fix` `Refactor` `Chore` `Test` `Docs`
- 제목은 한글로, "무엇을/왜"를 간결히 작성합니다. 예) `[Refactor] : 🔧 VG 테스트 코드를 운영 VG 소스에 정합`
- 하단 트레일러: 본문 끝에 `Signed-off-by: 최재영 <choi.debugs.y@gmail.com>` (Claude Co-Authored-By는 넣지 않음).
